; Load-path directories this checkout needs. Read, never evaluated.
; vim: set filetype=otium :

; The prelude and expander are embedded at build time and deliberately absent.
(paths "examples/ray" "examples/demo")
